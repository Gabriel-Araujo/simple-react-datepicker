import {CSSProperties} from "react";

export interface CalendarStyles {
    Calendar: CSSProperties;
    CalendarTop: CSSProperties;
    CalendarTitle: CSSProperties;
    NavigationButtons: CSSProperties;
    CalendarTable: CSSProperties;
    CalendarTHead: CSSProperties;
    CalendarTHeadCell: CSSProperties;
    CalendarTBody: CSSProperties;
    CalendarTBodyCell: CSSProperties;
}